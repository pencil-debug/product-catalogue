apiVersion: v1
kind: Service
metadata:
  name: product-catalogue-service
spec:
  selector:
    app: product-catalogue
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: NodePort